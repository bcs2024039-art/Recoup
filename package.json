{
  "name": "recoup",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd frontend && npm install",
    "dev": "cd frontend && npm run dev",
    "dev:python": "cd backend && python -m uvicorn api:app --host 0.0.0.0 --port 8000 --reload",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start"
  }
}
